export type DoctorMovementResponseModel = {
    id: string;
    //  movementId: string;
    assessmentId: string;
     metadataId: string;
     name: string,
     startDate: string,
     endDate: string,
  };
  